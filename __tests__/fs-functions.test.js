const { mkdirp } = require('../lib/fs-functions');
const fs = require('fs');

// let filepath = './my/file/path/goes/here'
// describe('mkdirp', () => {
//     it('can create a directory and all of its parent directories', () => {
//         return mkdirp(filepath)
//             .then(result => {
//                 expect(result).toEqual('./my/file/path/path/goes/here');
//             });
//     });
// });


